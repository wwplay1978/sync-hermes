name: Sync Hermes Image to Aliyun

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - name: 1. Login to Aliyun
      run: |
        echo "!!Pa55word!!" | docker login --username="wwplay@gmail.com" --password-stdin crpi-g8pfju6vtmetiw0d.cn-shanghai.personal.cr.aliyuncs.com

    - name: 2. Pull Original Image
      run: |
        docker pull nousresearch/hermes-agent:latest

    - name: 3. Retag and Push to Aliyun
      run: |
        # 注意：这里必须是完整的阿里云长网址，不能带有空格和省略
        docker tag nousresearch/hermes-agent:latest ://aliyuncs.com
        docker push ://aliyuncs.com
